# Personal_Website

This repo contains a simple react app of my personal profile.
The "client" folder contains the profile pages.
The "server" folder contains a simple web server that returns a sample data
for api 'get' request /test/api.
